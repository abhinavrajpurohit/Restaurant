/*
* Test Cases for Mock API JSON
*/
import { Mockapiurl } from './mockapiurl';

describe('Mockapiurl', () => {
  it('should create an instance', () => {
    expect(new Mockapiurl()).toBeTruthy();
  });
});
